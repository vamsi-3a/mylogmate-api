"""ORM model registry.

Import ALL models here so Alembic can discover them for autogenerate.
Step 3 will populate this file as models are created.

Example:
    from app.db.models.user import User
    from app.db.models.context import Context
    from app.db.models.log_entry import LogEntry
    ...
"""
